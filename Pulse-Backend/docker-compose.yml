# Self-hosted Pulse backend.
#   docker compose up -d --build
# Secrets come from .env (never baked into the image — see .dockerignore).
services:
  pulse-backend:
    build: .
    ports:
      - "5000:5000"
    env_file: .env
    restart: unless-stopped
